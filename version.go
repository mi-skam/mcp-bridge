package main

// version is the single source of truth; extension.json must match (see version_test.go).
// Release: bump here and in extension.json, commit, `git tag -a v<version>`, push with --tags.
const version = "3.0.0"
